# 启动清单 V2

## 一、硬件采购

主板统一使用 `NUCLEO-F446RE`。不要购买型号相近但芯片不同的板子，以免教程、引脚和工程配置不一致。

| 物品 | 数量 | 用途 | 优先级 |
|---|---:|---|---|
| NUCLEO-F446RE | 1 | 主开发板，板载 ST-Link，项目二使用 bxCAN | 必买 |
| BME280 模块 | 1 | I2C 温湿度/气压采集 | 必买 |
| 0.96 寸 I2C OLED | 1 | 本地显示 | 必买 |
| SN65HVD230 3.3V CAN 收发器模块 | 1 | 把 MCU 的 CAN_TX/CAN_RX 转为 CAN_H/CAN_L | 必买 |
| CANable 兼容 USB-CAN | 1 | 电脑收发和记录 CAN 报文 | 必买 |
| 120 欧姆电阻 | 2 | CAN 总线两端终端匹配 | 必买 |
| 面包板、杜邦线、LED、电阻、按键 | 1 套 | 基础实验 | 必买 |
| 基础数字万用表 | 1 | 电压、通断、电阻和断电总线电阻排查 | 必买 |
| 8 通道逻辑分析仪 | 1 | UART/I2C/SPI 排错；不能替代 CAN 收发器 | 预算允许再买 |

预算控制在 300-600 元。购买前确认 BME280 和 SN65HVD230 模块为 3.3V 逻辑；CANable 兼容设备必须能在 Windows 下稳定收发 500 kbps、11 位标准帧。MAX3485 和 USB-RS485 不再购买。

## 二、软件安装顺序

1. MSYS2 + GCC + Make + GDB：用于 C 语言练习、自动测试和 Linux 精简实验。
2. STM32CubeIDE：从 STMicroelectronics 官方页面下载，用于 STM32 工程、下载和断点调试：`https://www.st.com/en/development-tools/stm32cubeide.html`。
3. 串口终端：用于 CLI 和调试日志。
4. CANable 对应驱动与上位机工具：设备到货后按卖家芯片型号确认，先完成回环或双节点收发再进入项目二。
5. WSL2 Ubuntu：只在 6 小时 Linux 实验前配置，不占用第一周主线时间。

Git、GCC、GDB 和 Make 已安装，不需要重复安装。当前 MSYS2 中 GNU Make 的程序名是 `mingw32-make.exe`，不是 `make.exe`。

PowerShell 验证命令：

```powershell
& 'C:\msys64\ucrt64\bin\gcc.exe' --version
& 'C:\msys64\ucrt64\bin\gdb.exe' --version
& 'C:\msys64\ucrt64\bin\mingw32-make.exe' --version
git --version
```

## 三、到货检查

- NUCLEO-F446RE 能被 CubeIDE 识别、下载并断点调试。
- BME280 能读到正确芯片 ID；确认模块不是外观相似的 BMP280。
- SN65HVD230 模块供电和逻辑电平正确，CAN_H/CAN_L 标识清楚。
- 两只终端电阻分别接在总线两端；断电后从 CAN_H 到 CAN_L 测得约 60 欧姆。
- 未上电前先用万用表排除 3.3V 与 GND 短路。

## 四、第一天完成标准

- [ ] 确认硬件型号和预算，缺少的物品完成下单。
- [x] GCC 16.1.0、GDB 17.2、GNU Make 4.4.1 和 Git 2.55.0 已验证可运行。
- [ ] 运行一次环形缓冲区测试，初始失败属于正常现象。
- [ ] 在跟踪表中填写第一天记录和证据路径。
- [ ] 建立新的项目仓库，但不上传无法解释的代码。
