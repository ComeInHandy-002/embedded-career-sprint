# 8 月 17 日：C 基础诊断与第一次提交

今天不学 CAN，不看 FreeRTOS，也不追求写完环形缓冲区。目标是确认 C 基础真实水平，并建立第一条可以复查的代码证据。

## 6 小时安排

| 时间 | 任务 | 完成证据 |
|---|---|---|
| 0:00-0:30 | 阅读接口和测试；运行两组基线测试 | 记录诊断 0/6、环形缓冲区 0/8 |
| 0:30-2:00 | 不看答案复习变量、数组、函数、循环、返回值和编译警告 | 在纸上写出 4 个诊断函数的输入、输出和失败路径 |
| 2:00-4:00 | 独立实现 `diagnostic/day01_baseline.c` | 诊断测试达到 6/6，零警告 |
| 4:00-5:00 | 关闭资料，重新口述每个函数为什么这样处理空指针和长度 | 能解释失败时为什么不能修改输出参数 |
| 5:00-5:30 | 只实现 `src/ring_buffer.c` 中的 `rb_init` | 环形缓冲区第 1 个测试通过 |
| 5:30-6:00 | Git 提交、填写跟踪表和一句复盘 | 提交哈希、真实用时、测试结果 |

## 运行命令

在 `01_C恢复训练` 目录打开 PowerShell：

```powershell
powershell -ExecutionPolicy Bypass -File .\run_day01_diagnostic.ps1
powershell -ExecutionPolicy Bypass -File .\run_tests.ps1
```

不需要安装第三方库。编译器已经验证可用。

本地仓库已经初始化。如果第一次提交提示身份缺失，只在当前仓库设置你自己的真实信息：

```powershell
git config user.name "朱文杰"
git config user.email "你实际使用的邮箱"
```

不要把示例邮箱原样写入配置，也不要在没有检查改动的情况下直接提交。

## 独立完成规则

- 可以查看编译器和测试输出，但先不要搜索现成实现，也不要让 AI 代写函数。
- 不修改测试文件，不删除失败分支，不降低编译警告等级。
- 一次只修复第一个失败原因；每次运行都观察通过数量是否真实增加。
- 卡住 30 分钟后，记录“我认为原因是什么、已经试过什么”，再把代码和输出发来请求提示。

## 今天的验收

- [ ] `run_day01_diagnostic.ps1` 显示 6/6。
- [ ] `run_tests.ps1` 至少第 1 项“初始化为空”通过。
- [ ] 能解释数组长度为什么必须由调用者传入。
- [ ] 能解释空指针检查为什么要放在解引用之前。
- [ ] 工作区中没有复制来的、本人无法解释的实现。
- [ ] 跟踪表填写了真实用时、证据路径和一句复盘。

完成后把以下三样内容发来：两个测试结果、`day01_baseline.c`、`ring_buffer.c`。下一轮只根据你的真实代码做评审，不直接给完整答案。
