# 8 月 19 日：指针基础与写入路径

今天的目标不是看完一套指针课程，而是用运行结果证明自己能正确使用输入指针、输出指针、数组长度和结构体指针。

## 6 小时安排

| 时间 | 任务 | 验收证据 |
|---|---|---|
| 0:00-0:30 | 闭卷画出变量、地址和指针的关系；复述 `*` 与 `&` | 能解释 `int value`、`&value`、`int *pointer`、`*pointer` |
| 0:30-2:00 | 独立完成 `d2_swap_int` | 前 2 项测试通过 |
| 2:00-3:30 | 独立完成 `d2_min_max` | 前 5 项测试通过，失败路径不修改输出 |
| 3:30-4:15 | 独立完成 `d2_string_length` | 第 2 天指针练习达到 6/6 |
| 4:15-5:30 | 实现 `rb_push`、`rb_size`、`rb_is_full` | 写入数量和写满拒绝测试通过 |
| 5:30-6:00 | 关闭资料口述状态变化，提交本人代码 | Git 哈希、测试结果和一句复盘 |

## 运行命令

在 `01_C恢复训练` 目录运行：

```powershell
.\run_day02_pointers.ps1
.\run_tests.ps1
```

## 独立完成规则

- 只修改 `diagnostic/day02_pointers.c` 和已经指定的环形缓冲区函数。
- 不修改头文件、测试文件或编译脚本。
- 每次只修第一个失败原因，测试增加后再进入下一个函数。
- 卡住 20 分钟后，先写出输入、输出、非法情况和已经尝试的思路，再请求一级提示。
- AI 不直接完成这些核心函数；所有可提交实现必须由本人写出并解释。

## 今天必须能回答

1. `&value` 和 `*pointer` 分别表示什么？
2. 为什么数组传入函数后还要单独传入 `length`？
3. 为什么求最小值和最大值要先用局部变量，最后再写输出指针？
4. C 字符串为什么可以通过查找 `\0` 得到长度？
5. `tail` 在环形缓冲区中表示什么，为什么需要回绕？
