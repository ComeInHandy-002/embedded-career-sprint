# 首日提交记录

## 基本信息

- 日期：8.18
- 开始时间：14:00
- 结束时间：18:10
- 高效学习时长：4
- Git 提交哈希：5efe0b9

## 测试证据

- 首日诊断开始结果：0/6
- 首日诊断结束结果：6/6
- 环形缓冲区开始结果：0/8
- 环形缓冲区结束结果：2/8
- 是否严格编译零警告：是

## 本人完成的内容

- 修改了哪些函数：d1_sum，d1_max，d1_reverse,d1_count_equal
- 哪个失败用例花费时间最长：d1_max
- 最终发现的根因：没有考虑到数组全负的情况
- 是否查看分级提示；看到第几级：没有

## 脱离资料复现

不用看代码，回答：

1. `d1_max` 为什么不能把候选最大值初始化为 0？数组全负
2. `d1_reverse` 如何避免长度为 0 时的无符号下溢？设置安全壁垒,先判断 length 是否小于 2，长度为 0 或 1 时直接返回；
   只有确认长度足够时，才计算 length - 1 - i，避免 size_t 下溢。
3. 输出参数为什么只在成功路径写入？因为失败路径没输出
4. `rb_init` 为什么不必清空整个 `data` 数组？因为重置头尾指针，就相当于重置了数组没必要清空这个‘data’   head、tail、count 决定哪些 data 元素有效。初始化后 count 为 0，
   旧 data 内容不会被读取；后续写入会覆盖真正有效的位置，因此无需逐个清零。


## 一句复盘

今天最需要补的 C 基础是：数组和指针还需要加强，安全意识有待提高，C语言代码细节有待提高
