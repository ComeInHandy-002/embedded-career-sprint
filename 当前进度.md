# 当前进度

更新时间：2026 年 8 月 19 日。

## 已完成

- 已建立 8 月 17 日至 11 月 30 日的个性化冲刺工作区。
- 已将主线调整为 `STM32 + FreeRTOS + CAN`，开发/测试投递目标为 80:20。
- 已按 CAN 零基础重新定义项目二的学习前置、五任务结构、四个 CAN ID 和异常测试。
- 已增加 16 小时数模电与仪器复习、12 小时 CAN 零基础入门/汽车电子经历恢复，并把 Linux 压缩为 6 小时。
- 已安装 MSYS2、GCC 16.1.0、GDB 17.2、GNU Make 4.4.1（可执行文件名为 `mingw32-make.exe`）。
- 已建立 C 语言闭卷测试和环形缓冲区 8 项自动测试；骨架在 `-Wall -Wextra -Werror` 下编译成功。
- 首日 C 诊断已由本人完成并通过 6/6；环形缓冲区已完成初始化、写入、数量和满状态逻辑，当前通过 4/8。
- 已删除计划中的 RS485/Modbus 路线和相关采购项。
- 已核验并录入 40 条长三角岗位样本，其中 33 条本科可投、7 条明确为硕士门槛。
- 已增加首日 C 基础诊断的 6 项自动测试，并已由本人完成 6/6。
- 已建立第 2 天指针练习的 6 项自动测试，并由本人完成 6/6、严格编译零警告。
- 已在 2026-08-17 重新核验首轮岗位来源，并建立 10 条分阶段投递队列；尚未发生真实投递。
- 已将杭州视芯岗位匹配度从 5 降为 2：岗位经验不限，但需求专业只列电子信息和测控，计算机专业可能被筛选。
- 已按实际安排将正式训练起点顺延到 8 月 18 日；各学习阶段顺延一天，11 月 30 日求职终点不变。

## 当前能力边界

- CAN 目前只知道“CAN 通常快、LIN 通常慢”，按零基础学习，简历不得写“熟悉 CAN”。
- 星宇实习可证明的是抓取 CAN 报文、基础仪器排查、执行既有用例和问题反馈；不能扩写为自动化或独立设计方案。
- 两个公开求职项目尚未完成，当前不能作为已完成成果投递。
- 旧 vibe coding 项目不进入新简历。
- 环形缓冲区仍缺少 `rb_pop`、`rb_is_empty`、`rb_peek`；当前测试为 4/8，不能作为已完成项目成果。

## 尚未完成

- STM32CubeIDE 尚未安装；需从 STMicroelectronics 官网获取。
- NUCLEO-F446RE、BME280、OLED、SN65HVD230、CANable 和终端电阻需要本人确认或下单。
- 环形缓冲区核心逻辑需要本人独立实现并通过 8/8 测试。
- 两段实习的准确日期、岗位、部门和量化信息需要本人核对。
- 投递和面试数据只能在真实行动后填写，不得预填或虚构。

## 下一步，只做这三件事

1. 按 `01_C恢复训练/今日任务_2026-08-20.md` 完成 `rb_pop`、`rb_is_empty`、`rb_peek`，将环形缓冲区提升到 8/8。
2. 关闭资料口述 `head`、`tail`、`count` 的状态变化，再复习结构体、枚举、`typedef` 和结构体指针。
3. 补齐两段实习的准确年月和岗位信息；按 `05_求职/首轮投递清单_2026-08-17.md` 准备首轮真实投递。

CAN 学习从 9 月进入，不要因为实习时接触过 CANoe 就跳过基础，也不要提前占用 C 恢复时间。
